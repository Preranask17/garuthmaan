"use client";

import React from "react";
import { motion } from "framer-motion";

const Milestones = () => {
  return (
    <section id="milestones" className="py-24 max-w-7xl mx-auto px-4">
      <h3 className="text-3xl sm:text-5xl font-bold tracking-tight text-white mb-16 text-center">MILESTONES</h3>
    </section>
  );
};

export default Milestones;